id: ts_project_mode_eval

mode: project
language: typescript

project:
  path: generated_project

setup:
  commands: []

output:
  path: ../../runs/ts_project_mode_results.jsonl

runner:
  workers: 1

sandbox:
  timeout: 30
  memory: 512m
  cpus: "1"
  pids_limit: 256
  network: false

viewer:
  enabled: true
  out: ../../runs/ts_project_mode.viewer.html
  open: false
