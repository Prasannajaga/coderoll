{"id": "py_complex_good", "code": "from __future__ import annotations\n\nfrom collections import Counter\n\n\ndef normalize_user_ids(values: list[str]) -> list[str]:\n    normalized: list[str] = []\n    for value in values:\n        text = str(value).strip().lower()\n        if text:\n            normalized.append(text)\n    return normalized\n\n\ndef top_k_frequencies(values: list[str], k: int) -> list[tuple[str, int]]:\n    if k <= 0:\n        return []\n    counter = Counter(normalize_user_ids(values))\n    ranked = sorted(counter.items(), key=lambda item: (-item[1], item[0]))\n    return ranked[:k]\n\n\ndef rolling_average(values: list[float], window: int) -> list[float]:\n    if window <= 0:\n        raise ValueError(\"window must be > 0\")\n    if window > len(values):\n        return []\n\n    out: list[float] = []\n    for start in range(0, len(values) - window + 1):\n        chunk = values[start : start + window]\n        out.append(round(sum(chunk) / window, 3))\n    return out\n", "tests": "import pytest\n\nfrom solution import normalize_user_ids, rolling_average, top_k_frequencies\n\n\ndef test_normalize_user_ids_filters_blanks_and_lowercases() -> None:\n    values = [\"  Alice  \", \"\", \"BOB\", \"   \", \"carol\", \"Alice\"]\n    assert normalize_user_ids(values) == [\"alice\", \"bob\", \"carol\", \"alice\"]\n\n\ndef test_top_k_frequencies_orders_by_count_then_alpha() -> None:\n    values = [\"beta\", \"alpha\", \"beta\", \"alpha\", \"gamma\"]\n    assert top_k_frequencies(values, 2) == [(\"alpha\", 2), (\"beta\", 2)]\n\n\ndef test_rolling_average_window_two() -> None:\n    assert rolling_average([2, 4, 6, 8], 2) == [3.0, 5.0, 7.0]\n\n\ndef test_rolling_average_window_equal_length() -> None:\n    assert rolling_average([1, 2, 3, 4], 4) == [2.5]\n\n\ndef test_rolling_average_large_window_returns_empty() -> None:\n    assert rolling_average([1, 2], 3) == []\n\n\ndef test_invalid_window_raises() -> None:\n    with pytest.raises(ValueError):\n        rolling_average([1, 2, 3], 0)\n", "metadata": {"kind": "golden"}}
{"id": "py_complex_partial", "code": "from __future__ import annotations\n\nfrom collections import Counter\n\n\ndef normalize_user_ids(values: list[str]) -> list[str]:\n    return [str(v).strip().lower() for v in values if str(v).strip()]\n\n\ndef top_k_frequencies(values: list[str], k: int) -> list[tuple[str, int]]:\n    if k <= 0:\n        return []\n    counter = Counter(normalize_user_ids(values))\n    # BUG: ties depend on insertion order, not alphabetical id.\n    ranked = sorted(counter.items(), key=lambda item: -item[1])\n    return ranked[:k]\n\n\ndef rolling_average(values: list[float], window: int) -> list[float]:\n    if window <= 0:\n        raise ValueError(\"window must be > 0\")\n    if window > len(values):\n        return []\n    return [round(sum(values[i : i + window]) / window, 3) for i in range(len(values) - window + 1)]\n", "tests": "import pytest\n\nfrom solution import normalize_user_ids, rolling_average, top_k_frequencies\n\n\ndef test_normalize_user_ids_filters_blanks_and_lowercases() -> None:\n    values = [\"  Alice  \", \"\", \"BOB\", \"   \", \"carol\", \"Alice\"]\n    assert normalize_user_ids(values) == [\"alice\", \"bob\", \"carol\", \"alice\"]\n\n\ndef test_top_k_frequencies_orders_by_count_then_alpha() -> None:\n    values = [\"beta\", \"alpha\", \"beta\", \"alpha\", \"gamma\"]\n    assert top_k_frequencies(values, 2) == [(\"alpha\", 2), (\"beta\", 2)]\n\n\ndef test_rolling_average_window_two() -> None:\n    assert rolling_average([2, 4, 6, 8], 2) == [3.0, 5.0, 7.0]\n\n\ndef test_rolling_average_window_equal_length() -> None:\n    assert rolling_average([1, 2, 3, 4], 4) == [2.5]\n\n\ndef test_rolling_average_large_window_returns_empty() -> None:\n    assert rolling_average([1, 2], 3) == []\n\n\ndef test_invalid_window_raises() -> None:\n    with pytest.raises(ValueError):\n        rolling_average([1, 2, 3], 0)\n", "metadata": {"kind": "partial"}}
{"id": "py_complex_timeout", "code": "from __future__ import annotations\n\n\ndef normalize_user_ids(values: list[str]) -> list[str]:\n    return [str(v).strip().lower() for v in values if str(v).strip()]\n\n\ndef top_k_frequencies(values: list[str], k: int) -> list[tuple[str, int]]:\n    counts: dict[str, int] = {}\n    for value in normalize_user_ids(values):\n        counts[value] = counts.get(value, 0) + 1\n    return sorted(counts.items(), key=lambda item: (-item[1], item[0]))[:k]\n\n\ndef rolling_average(values: list[float], window: int) -> list[float]:\n    # BUG: infinite loop for normal test inputs.\n    out: list[float] = []\n    i = 0\n    while i <= len(values) - window:\n        chunk = values[i : i + window]\n        out.append(sum(chunk) / window)\n    return out\n", "tests": "import pytest\n\nfrom solution import normalize_user_ids, rolling_average, top_k_frequencies\n\n\ndef test_normalize_user_ids_filters_blanks_and_lowercases() -> None:\n    values = [\"  Alice  \", \"\", \"BOB\", \"   \", \"carol\", \"Alice\"]\n    assert normalize_user_ids(values) == [\"alice\", \"bob\", \"carol\", \"alice\"]\n\n\ndef test_top_k_frequencies_orders_by_count_then_alpha() -> None:\n    values = [\"beta\", \"alpha\", \"beta\", \"alpha\", \"gamma\"]\n    assert top_k_frequencies(values, 2) == [(\"alpha\", 2), (\"beta\", 2)]\n\n\ndef test_rolling_average_window_two() -> None:\n    assert rolling_average([2, 4, 6, 8], 2) == [3.0, 5.0, 7.0]\n\n\ndef test_rolling_average_window_equal_length() -> None:\n    assert rolling_average([1, 2, 3, 4], 4) == [2.5]\n\n\ndef test_rolling_average_large_window_returns_empty() -> None:\n    assert rolling_average([1, 2], 3) == []\n\n\ndef test_invalid_window_raises() -> None:\n    with pytest.raises(ValueError):\n        rolling_average([1, 2, 3], 0)\n", "metadata": {"kind": "timeout"}}
{"id": "py_complex_syntax_error", "code": "def normalize_user_ids(values)\n    return [v.strip().lower() for v in values]\n\n\ndef top_k_frequencies(values, k):\n    return []\n\n\ndef rolling_average(values, window):\n    return []\n", "tests": "import pytest\n\nfrom solution import normalize_user_ids, rolling_average, top_k_frequencies\n\n\ndef test_normalize_user_ids_filters_blanks_and_lowercases() -> None:\n    values = [\"  Alice  \", \"\", \"BOB\", \"   \", \"carol\", \"Alice\"]\n    assert normalize_user_ids(values) == [\"alice\", \"bob\", \"carol\", \"alice\"]\n\n\ndef test_top_k_frequencies_orders_by_count_then_alpha() -> None:\n    values = [\"beta\", \"alpha\", \"beta\", \"alpha\", \"gamma\"]\n    assert top_k_frequencies(values, 2) == [(\"alpha\", 2), (\"beta\", 2)]\n\n\ndef test_rolling_average_window_two() -> None:\n    assert rolling_average([2, 4, 6, 8], 2) == [3.0, 5.0, 7.0]\n\n\ndef test_rolling_average_window_equal_length() -> None:\n    assert rolling_average([1, 2, 3, 4], 4) == [2.5]\n\n\ndef test_rolling_average_large_window_returns_empty() -> None:\n    assert rolling_average([1, 2], 3) == []\n\n\ndef test_invalid_window_raises() -> None:\n    with pytest.raises(ValueError):\n        rolling_average([1, 2, 3], 0)\n", "metadata": {"kind": "eval_error"}}
{"id": "py_complex_slow_pass", "code": "from __future__ import annotations\n\nimport time\nfrom collections import Counter\n\n\ndef normalize_user_ids(values: list[str]) -> list[str]:\n    out: list[str] = []\n    for value in values:\n        text = str(value).strip().lower()\n        if text:\n            out.append(text)\n    return out\n\n\ndef top_k_frequencies(values: list[str], k: int) -> list[tuple[str, int]]:\n    counter = Counter(normalize_user_ids(values))\n    return sorted(counter.items(), key=lambda item: (-item[1], item[0]))[:k]\n\n\ndef rolling_average(values: list[float], window: int) -> list[float]:\n    if window <= 0:\n        raise ValueError(\"window must be > 0\")\n    if window > len(values):\n        return []\n    out: list[float] = []\n    for start in range(0, len(values) - window + 1):\n        time.sleep(0.03)\n        chunk = values[start : start + window]\n        out.append(round(sum(chunk) / window, 3))\n    return out\n", "tests": "import pytest\n\nfrom solution import normalize_user_ids, rolling_average, top_k_frequencies\n\n\ndef test_normalize_user_ids_filters_blanks_and_lowercases() -> None:\n    values = [\"  Alice  \", \"\", \"BOB\", \"   \", \"carol\", \"Alice\"]\n    assert normalize_user_ids(values) == [\"alice\", \"bob\", \"carol\", \"alice\"]\n\n\ndef test_top_k_frequencies_orders_by_count_then_alpha() -> None:\n    values = [\"beta\", \"alpha\", \"beta\", \"alpha\", \"gamma\"]\n    assert top_k_frequencies(values, 2) == [(\"alpha\", 2), (\"beta\", 2)]\n\n\ndef test_rolling_average_window_two() -> None:\n    assert rolling_average([2, 4, 6, 8], 2) == [3.0, 5.0, 7.0]\n\n\ndef test_rolling_average_window_equal_length() -> None:\n    assert rolling_average([1, 2, 3, 4], 4) == [2.5]\n\n\ndef test_rolling_average_large_window_returns_empty() -> None:\n    assert rolling_average([1, 2], 3) == []\n\n\ndef test_invalid_window_raises() -> None:\n    with pytest.raises(ValueError):\n        rolling_average([1, 2, 3], 0)\n", "metadata": {"kind": "slow"}}
{"id": "py_complex_bad", "code": "def normalize_user_ids(values: list[str]) -> list[str]:\n    return []\n\n\ndef top_k_frequencies(values: list[str], k: int) -> list[tuple[str, int]]:\n    return [(\"nobody\", 1)]\n\n\ndef rolling_average(values: list[float], window: int) -> list[float]:\n    return [0.0]\n", "tests": "import pytest\n\nfrom solution import normalize_user_ids, rolling_average, top_k_frequencies\n\n\ndef test_normalize_user_ids_filters_blanks_and_lowercases() -> None:\n    values = [\"  Alice  \", \"\", \"BOB\", \"   \", \"carol\", \"Alice\"]\n    assert normalize_user_ids(values) == [\"alice\", \"bob\", \"carol\", \"alice\"]\n\n\ndef test_top_k_frequencies_orders_by_count_then_alpha() -> None:\n    values = [\"beta\", \"alpha\", \"beta\", \"alpha\", \"gamma\"]\n    assert top_k_frequencies(values, 2) == [(\"alpha\", 2), (\"beta\", 2)]\n\n\ndef test_rolling_average_window_two() -> None:\n    assert rolling_average([2, 4, 6, 8], 2) == [3.0, 5.0, 7.0]\n\n\ndef test_rolling_average_window_equal_length() -> None:\n    assert rolling_average([1, 2, 3, 4], 4) == [2.5]\n\n\ndef test_rolling_average_large_window_returns_empty() -> None:\n    assert rolling_average([1, 2], 3) == []\n\n\ndef test_invalid_window_raises() -> None:\n    with pytest.raises(ValueError):\n        rolling_average([1, 2, 3], 0)\n", "metadata": {"kind": "failing"}}
