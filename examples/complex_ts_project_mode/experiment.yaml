id: complex_ts_project_mode_eval

mode: project
language: typescript

project:
  path: generated_project

setup:
  commands: []

output:
  path: ../../runs/complex_ts_project_mode_results.jsonl

runner:
  workers: 1

sandbox:
  timeout: 35
  memory: 512m
  cpus: "1"
  pids_limit: 256
  network: false

viewer:
  enabled: true
  out: ../../runs/complex_ts_project_mode.viewer.html
  open: false
