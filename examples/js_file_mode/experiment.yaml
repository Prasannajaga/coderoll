id: js_file_mode_eval

mode: file
language: javascript

candidates:
  path: candidates.jsonl
  type: jsonl

setup:
  commands: []

output:
  path: ../../runs/js_file_mode_results.jsonl

runner:
  workers: 2

sandbox:
  timeout: 10
  memory: 512m
  cpus: "1"
  pids_limit: 128
  network: false

viewer:
  enabled: true
  out: ../../runs/js_file_mode.viewer.html
  open: false
